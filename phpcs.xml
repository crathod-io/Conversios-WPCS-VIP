<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" name="WordPress-VIP-Go" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/squizlabs/PHP_CodeSniffer/master/phpcs.xsd">
	<description>WordPress VIP Go Coding Standards by Crathod</description>
	
	<file>.</file>
	
	<!-- Exclude WP Core folders and files from being checked. -->
	<exclude-pattern>/wp-admin/*</exclude-pattern>
	<exclude-pattern>/wp-includes/*</exclude-pattern>
	<exclude-pattern>/wp-*.php</exclude-pattern>
	<exclude-pattern>/xmlrpc.php</exclude-pattern>

	<!-- Exclude the Composer Vendor directory. -->
	<exclude-pattern>/vendor/*</exclude-pattern>

	<!-- Exclude the Node Modules directory. -->
	<exclude-pattern>/node_modules/*</exclude-pattern>

	<!-- Exclude minified Javascript files. -->
	<exclude-pattern>*.min.js</exclude-pattern>
	
	<!-- Conversios custom rule by crathod -->
	<rule ref="WordPress">
		<exclude name="WordPress.WhiteSpace.ControlStructureSpacing.NoSpaceBeforeCloseParenthesis" />
		<exclude name="WordPress.WhiteSpace.ControlStructureSpacing.*" />
	</rule>

</ruleset>
